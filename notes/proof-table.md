# 六类结论证明表（Day 4）

- **共建**：A 刘威（概率/空间/参数列）· B 张书铖（不变式/操作/实现列）  
- **复核**：C（可读性与原文定位）  
- **规则**：每行区分「论文命题 / 小组解释 / 未验证」；禁止用 8 元素示例代替一般证明。

> 本文件当前以 **B 提交的操作与不变式列** 为主；标「待 A」处请刘威补概率求和与空间峰值精算。

---

## 总表

| 结论 | 精确命题 | 所需不变式/引理（B） | 证明步骤（分工） | 原文位置 | 适用条件 | 当前缺口 | 复核人 |
|---|---|---|---|---|---|---|---|
| 无 FN | 无 failure 时，已插入 key 的 Lookup=YES | 写入前缀；迁移不丢串；查询四结构覆盖（意图） | B：三种状态论证见 `proof-notes.md` §2；A：确认与随机性无关（确定性） | §3.1；Claim 13 末；Thm 10.4 | `¬failed` | 存储归纳 vs `i★` 未统一 | C |
| FP≤ε | 无 failure 时，非成员 Lookup=YES 的概率 ≤ε | 式 (2)； pairwise；阶段/键并合 | **待 A** 写逐项求和；B：确认对象是各阶段已存前缀集合 | §3.1 式 (2) | `¬failed`；哈希独立 | A 精算稿 | C |
| 空间界 | Thm 10 主体位数 + `u^c` | Lemma 11 空间；仅常数层 `D/T` 并存 | **待 A** 主项/低阶/峰值；B：确认无 `log n` 个全量 filter | Lemma 11；Thm 10；Claim 13 | `n=ω(log u)` 等 | 峰值 Q3；下标 | C |
| 查询时间 | 每次 Lookup worst-case O(1) | 至多 4 次底层 query；底层 O(1) | B：`proof-notes.md` §3；伪代码 `Lookup` | Claim 13 末；Thm 10.2 | `¬failed` | 无（控制流） | A |
| 插入时间 | 每次 Insert worst-case O(1) | 1×insert + 10×MigrateOneStep；底层 O(1) | B：`proof-notes.md` §4；伪代码 `Insert` | Claim 13；§1.4；Thm 10.2 | `¬failed`；字典接口 | 字面 10=Q3 | A |
| 高概率 | 任意插入序列上曾 failure 的概率 ≤δ=u^{-C} | 每层 `D_i` failure≤u^{-2C}；≤log u 层并合；§5 式(3)(4)(5) | **待 A** 逐项并合；B：事件落点（桶/指纹/hd∘hs） | Claim 13；§5；Thm 10.3 | 预计算随机比特；序列任意 | 式(4)(5) 精读 | B/C |

---

## C 第二轮可追溯性审阅（2026-07-21）

| 结论 | C 核查结果 | 状态 |
|---|---|---|
| 无 FN | 三状态直觉可读，但 `i★` 换层时的存储覆盖归纳未关闭；迁移目标写入 failure 也未传播 | Q1/Q3 |
| FP≤ε | 需补按插入阶段的逐项求和，并说明 `T` 一变二标记如何纳入并合 | 待 A |
| 空间界 | 查询四结构不能替代分配峰值证明；需计初始化下一层、待销毁旧层和迁移临时态 | Q3 / 待 A |
| 查询时间 | 四次底层 query 清楚；`i=0` 的负下标和 `ell(0)` 需有限规模特判 | 小边界待 B |
| 插入时间 | “推进初始化直至可写”可能集中线性工作；应改为上一阶段就绪不变式 | 高优先级待 B |
| 高概率 | 式 (3)(4)(5) 到整段 `u^{-C}` 的逐事件/逐层并合仍未写出 | 待 A，B/C 复核 |

完整意见与伪代码审阅见 `discussions/review-day4-C.md`。本表内容已经成员 C 审核；其中标为 Q1/Q3 或待 A/B 的项目仍须依据后续证据关闭。

---

## B 行展开：无 FN

| 字段 | 内容 |
|---|---|
| 论文命题 | 无 fail 时 query 正确（含成员必 YES） |
| 小组解释 | 见 `proof-notes.md` §2.3 插入前/中/后 |
| 未验证 | 半开区间存储不变式形式证明 |
| 实现列 | `pseudocode.md`：Insert 写入 + MigrateOneStep 立即转存 + Lookup 四查 |

## B 行展开：查询时间

| 字段 | 内容 |
|---|---|
| 论文命题 | worst-case O(1) |
| 小组解释 | 4 次 query，无循环依赖 n |
| 未验证 | 无（在黑盒 O(1) 前提下） |
| 实现列 | `Lookup` |

## B 行展开：插入时间

| 字段 | 内容 |
|---|---|
| 论文命题 | worst-case O(1) |
| 小组解释 | 常数轮维护；渐近配平 Q1；字面 10 Q3 |
| 未验证 | 隐藏常数会计 |
| 实现列 | `Insert` + `MigrateOneStep` |

## B 行展开：高概率（结构侧）

| 字段 | 内容 |
|---|---|
| 论文命题 | 总 failure ≤δ |
| 小组解释 | 每 `D_i` 对 core-set 分布 failure≤u^{-2C}`，对 i=1..log u 并合 |
| 未验证 | §5 各事件→u^{-2C} 的完整推导（A） |
| 实现列 | `failed` 标志；无自动 rebuild API |

---

## 更新日志

| 日期 | 作者 | 变更 |
|---|---|---|
| 2026-07-22 | B | 初建表；填不变式/操作/实现列与三行展开 |
| （待填） | A | 概率求和、空间峰值、式(3)(4)(5) |
| （待填） | C | 可读性审阅 |
