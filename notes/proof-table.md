# 六类结论证明表（Day 4）

- **共建**：A 刘威（概率/空间/参数列）· B 张书铖（不变式/操作/实现列）  
- **复核**：C（可读性与原文定位）  
- **规则**：每行区分「论文命题 / 小组解释 / 未验证」；禁止用 8 元素示例代替一般证明。


---

## 总表

| 结论 | 精确命题 | 所需不变式/引理（B） | 证明步骤（分工） | 原文位置 | 适用条件 | 当前缺口 | 复核人 |
|---|---|---|---|---|---|---|---|
| 无 FN | 无 failure 时，已插入 key 的 Lookup=YES | 写入前缀；迁移不丢串；查询四结构覆盖（意图） | B：三种状态论证见 `proof-notes.md` §2；A：确认与随机性无关（确定性） | §3.1；Claim 13 末；Thm 10.4 | `¬failed` | 存储归纳 vs `i★` 未统一 | C |
| FP≤ε | 无 failure 时，非成员 Lookup=YES 的概率 ≤ε | 式 (2)；阶段内元素与跨阶段 union bound | A：`Σ_i2^i2^{-ℓ_i}=Σ_i ε/(4i log log u)<ε`；B：迁移不新增预算 | §3.1 式 (2) | `¬failed`；全局高独立哈希 | 求和已补；存储下标不影响预算但仍 Q1 | C |
| 空间界 | Thm 10 主体位数 + `u^c` | Lemma 11 空间；相邻 `D/T` 生命周期流水线 | A：代入 `ℓ_{⌈log n⌉}`，分主项、低阶项和预计算表；B：确认无 `log n` 个全量 filter | Lemma 11；Thm 10；Claim 13 | `n=ω(log u),n<u` | 位级临时峰值常数仍 Q3 | C |
| 查询时间 | 每次 Lookup worst-case O(1) | 至多 4 次底层 query；底层 O(1) | B：`proof-notes.md` §3；伪代码 `Lookup` | Claim 13 末；Thm 10.2 | `¬failed` | 无（控制流） | A |
| 插入时间 | 每次 Insert worst-case O(1) | 1×insert + 10×MigrateOneStep；底层 O(1) | B：`proof-notes.md` §4；伪代码 `Insert` | Claim 13；§1.4；Thm 10.2 | `¬failed`；字典接口 | 字面 10=Q3 | A |
| 高概率 | 任意插入序列上曾 failure 的概率 ≤δ=u^{-C} | 每层 `D_i` failure≤u^{-2C}；≤log u 层并合；§5 式(3)(4)(5) | A：三类底层事件经式(5)并合，再算 `(log u)u^{-2C}≤u^{-C}`；B：事件落点 | Claim 13；§5；Thm 10.3 | 预计算随机比特；序列任意 | 自适应对手口径不扩张 | B/C |

---

## C 第二轮可追溯性审阅

| 结论 | C 核查结果 | 状态 |
|---|---|---|
| 无 FN | 三状态直觉可读，但 `i★` 换层时的存储覆盖归纳未关闭；迁移目标写入 failure 也未传播 | Q1/Q3 |
| FP≤ε | 原意见：需补按插入阶段的逐项求和，并说明 `T` 一变二标记如何纳入并合。A 已在新笔记 §2 回应 | **已修订**；整合稿 §8.2 按写入阶段计费，迁移不重复计费 |
| 空间界 | 原意见：需计初始化下一层、待销毁旧层和迁移临时态。A 已补生命周期量级解释 | 位级峰值仍 Q3 |
| 查询时间 | 四次底层 query 清楚；`i=0` 的负下标和 `ell(0)` 需有限规模特判 | **已修订**；B 章 §3 与整合稿 §6.3 将小规模作为有限基例 |
| 插入时间 | “推进初始化直至可写”可能集中线性工作；应改为上一阶段就绪不变式 | **已修订**；B 章 §4.3 禁止单次 while，字面 10 仍为 Q3 |
| 高概率 | A 已补式 (3)(4)(5) 单结构并合与跨层 `u^{-C}` 推导 | **已修订**；B 已抽查数字骨架，整合稿 §9.3 已展开事件口径 |

### Day 5 回应（B，2026-07-28）

- **插入时间 / 线性 initialize**：已在 `drafts/section-B-technique-and-proof.md` §4.3 与伪代码口径改为「阶段就绪不变式 + 每插常数轮推进」；禁止单次 Insert 内 while 到完成。
- **查询 `i=0` / `ℓ_0`**：B 章 §3 标明边界与待和 A 统一；证明表本行缺口降为「小边界待联合关闭」。
- **高概率数字复核**：抽查 A 笔记 `(log u)u^{-2C}≤u^{-C}`（`C>1`、`u` 充分大）骨架同意；Day 6 引用/强断言审计已将它作为带条件的已修订文本。


---

## B 行展开：无 FN

| 字段 | 内容 |
|---|---|
| 论文命题 | 无 fail 时 query 正确（含成员必 YES） |
| 小组解释 | 见 `proof-notes.md` §2.3 插入前/中/后 |
| 未验证 | 半开区间存储不变式形式证明 |
| 实现列 | `pseudocode.md`：Insert 写入 + MigrateOneStep 立即转存 + Lookup 四查 |

## B 行展开：查询时间

| 字段 | 内容 |
|---|---|
| 论文命题 | worst-case O(1) |
| 小组解释 | 4 次 query，无循环依赖 n |
| 未验证 | 无（在黑盒 O(1) 前提下） |
| 实现列 | `Lookup` |

## B 行展开：插入时间

| 字段 | 内容 |
|---|---|
| 论文命题 | worst-case O(1) |
| 小组解释 | 常数轮维护；渐近配平 Q1；字面 10 Q3 |
| 未验证 | 隐藏常数会计 |
| 实现列 | `Insert` + `MigrateOneStep` |

## B 行展开：高概率（结构侧）

| 字段 | 内容 |
|---|---|
| 论文命题 | 总 failure ≤δ |
| 小组解释 | 每 `D_i` 对 core-set 分布 failure≤u^{-2C}`，对 i=1..log u 并合 |
| 未验证 | 自适应查询对手的扩张不在本组结论内；式 (3)–(5) 到 `u^{-2C}` 已由 A 展开 |
| 实现列 | `failed` 标志；无自动 rebuild API |

