# Issue：Claim 13 阶段下标 `i★=⌈log n⌉` 与半开区间句如何统一

- **状态**：open  
- **创建**：2026-07-21 · 成员 B  
- **关联**：`core-components.md` 组件 4；A `review-day3-A.md` 意见 2；questions Q3  
- **复核人**：刘威；目标 Day 4

---

## 1. 冲突

| 来源 | 写法 | `n=3` 时 |
|---|---|---|
| 算法句 | `i ← ⌈log n⌉`，写 `D_i`，查四结构 | `i=2` → `D1,D2,T1,T2` |
| 证明段 | `n∈[2^i,2^{i+1})` 时数据在 `D_{i-1}…T_i` | 取 `i=1` → `D0,D1,T0,T1` |

二者不能共用同一自由变量 `i` 而不加说明。

## 2. 当前临时处理

引入 `i★(n)=⌈log n⌉` 与 `epoch(n)=⌊log n⌋`；图与流程只跟 `i★`；正式归纳不关闭。

## 3. 验收

1. 对照附录伪代码给出**唯一**阶段变量定义并重写不变式；或  
2. 确认排版/笔误并给出勘误句；或  
3. 证明两种下标在“查询覆盖”意义下等价（需形式证明）。

不得在未完成上列之一时把该不变式写进 Review 正文为“已证”。
