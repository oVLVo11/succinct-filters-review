# Day 4 交叉审阅记录

日期：2026-07-22


## B 审 A（概率 / 空间 / 参数）

被审：`notes/memberA/lemma-dependency-and-space.md`（A 的 Day 4 `probability-and-space-proof.md` 若稍后提交，请对下列意见一并回应）

### 评论 1：阶段指标与式 (2) 并合

- **位置**：空间/误报相关段落中的阶段下标 `i`
- **主张**：并合 `∑_i 2^i · 2^{-ℓ_i}` 中的 `i` 是**前缀长度公式的阶段指标**，必须与插入时使用的 `ℓ_i` 一致。
- **缺失**：若与 Claim 13 半开区间句混用同一字母，读者会对“每阶段键数 ≤2^i”产生歧义。
- **建议**：FP 证明固定“第 i 阶段插入的键使用 `ℓ_i`”，并显式引用式 (2)；存储层下标问题链到 issue-stage-index，勿在 FP 行静默假设已解决。
- **作者回应（A）**：接受。已在 `probability-and-space-proof.md` §2 固定按插入阶段求和，并明确迁移不产生新误报预算；存储归纳另挂 stage-index issue。
- **关闭依据**：概率求和意见关闭；阶段存储下标仍 Q1/Q3。

### 评论 2：空间并存与“只四结构”

- **位置**：引理笔记中 truth table / 相邻层 O(n) 论述
- **主张**：查询时间的“四结构”与空间峰值的“并存层”相关但不等同：下一层可能已 initialize 却不查询。
- **建议**：空间峰值段写清“可能已分配但未进入 Lookup 集合的层”；与 B `pseudocode.md` / A 审阅意见 1 对齐。
- **作者回应（A）**：接受。§4 已把已分配但未查询的下一层、待销毁旧层列入生命周期；位级临时峰值仍谨慎标 Q3。

### 评论 3：failure 并合的责任边界

- **位置**：式 (3)(4)(5) → u^{-2C} → δ
- **主张**：B 证明表已标明事件落点；完整求和属 A。在 A 稿未完成前，证明表高概率行不得标 Q0。
- **建议**：A 提交 `probability-and-space-proof.md` 后，B 再做第二轮数字核对。
- **作者回应（A）**：已在 §3 按“式 (3)–(5) 单个 `D` ≤`u^{-2C}`，再对 `O(log u)` 层并合至 `u^{-C}`”补齐；请 B/C 二次核对常数选择和事件口径。

---

## A 审 B（2026-07-28）

1. **写入层就绪**：接受 C 的高优先级意见。`Insert` 不应在单次调用中推进初始化“直至可用”；正文必须以前一阶段已完成当前层初始化为不变式，否则伪代码只能作控制流草图。
2. **迁移 failure**：`MigrateOneStep` 的所有目标写入都应检查返回值并传播 `failed`；“取出后立即写入”只在无 failure 执行上支持无漏报。
3. **有限规模边界**：`ell(0)`、`D[-1]/T[-1]` 必须用初始化特判规避，该修订不影响渐近界。
4. **通过部分**：四次底层查询支持查询 worst-case `O(1)`；每轮维护为常数次黑盒操作；rebuild 已正确标为非主定理 API。
5. **仍不关闭**：阶段下标形式归纳和字面常数 10。二者不应在 Day 5 写成小组已独立补证。

---

## C 审 A+B（第二轮已审核）

完整审阅见 `discussions/review-day4-C.md`。当前结论：

1. 六个证明目标都已留下至少一个可追溯性问题；FP、空间和高概率三行仍不能标 Q0。
2. B 伪代码有两个高优先级缺口：当前写入层未就绪时集中初始化可能破坏 worst-case `O(1)`；迁移写入的 failure 未检查和传播。
3. `ell(0)`、`i=0` 的负下标和 `AlreadyHasPrefix` 接口仍需伪代码级说明。
4. 上述意见已经成员 C 逐条确认并作为正式交叉审阅提交；A/B 回应前不自动关闭。

---

## B 对自身缺口的预声明（供 C）

1. `FinalizeStage` / `StartNewStage` 为抽象拆分，原文嵌在 Insert 维护中。  
2. `HandleFailureOrRebuild` 非主定理 API。  
3. 字面常数 10、阶段存储归纳仍为 Issue，正文不可写成已证。
